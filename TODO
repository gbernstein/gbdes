* write a radial TemplateMap
* have Templates serialize with their filenames instead of listing entries
* allow variable substitution (Device) into the DeviceMap specification.
* Update documentation: PixelMap.tex and the fitting classes.
* Document PhotoFit, MakeColors and all their parameters.
* Clean up instrument name translation
